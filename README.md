# AI Avatar Maker

一个现代化的 AI 头像生成器，支持上传照片并通过 AI 生成风格化头像。

## 项目结构

```
avatar/
├── avatar-maker-backend/   # 后端代理服务（Node.js + Express）
│   ├── src/
│   │   └── index.js       # 主服务文件
│   ├── package.json
│   └── README.md
├── avatar-maker-website/   # 前端应用（React + Vite + TypeScript）
│   ├── src/
│   │   ├── components/    # React 组件
│   │   ├── services/      # API 服务
│   │   ├── types/         # TypeScript 类型定义
│   │   └── utils/         # 工具函数
│   ├── package.json
│   └── .env              # 环境变量配置
└── start.sh              # 一键启动脚本
```

## 功能特性

✨ **前端功能**
- 图片上传（支持拖拽）
- 背景风格配置
- AI 模型参数设置
- 实时预览生成的头像
- 现代化玻璃拟态 UI 设计

🔧 **后端功能**
- AI API 请求代理（解决跨域问题）
- 支持大文件传输（最大 50MB）
- 健康检查接口

🔐 **数据安全**
- 用户的 API 配置（baseURL、apiToken、modelName）保存在客户端本地存储
- 后端不存储任何用户数据
- 仅负责转发请求

## 快速开始

### 方式一：使用启动脚本（推荐）

```bash
# 赋予执行权限
chmod +x start.sh

# 启动所有服务
./start.sh
```

### 方式二：手动启动

#### 1. 启动后端服务

```bash
cd avatar-maker-backend
npm install
npm run dev
```

后端服务运行在 `http://localhost:3001`

#### 2. 启动前端服务

```bash
cd avatar-maker-website
npm install
npm run dev
```

前端应用运行在 `http://localhost:5173`

## 使用说明

1. **配置 AI 模型**
   - 点击右上角的设置图标
   - 填写 AI 模型的 Base URL、API Token 和模型名称
   - 配置会自动保存在浏览器本地存储中

2. **上传用户照片**
   - 在左侧区域上传或拖拽要生成头像的照片

3. **配置背景**
   - 在中间区域选择背景类型（纯色/自动）
   - 可自定义背景颜色和元素

4. **生成头像**
   - 点击"生成头像"按钮
   - 等待 AI 处理
   - 查看和下载生成的头像

## 技术栈

### 前端
- **框架**: React 18 + TypeScript
- **构建工具**: Vite 5
- **样式**: TailwindCSS + 自定义动画
- **UI 组件**: Lucide React（图标）
- **状态管理**: React Hooks

### 后端
- **运行时**: Node.js
- **框架**: Express.js
- **跨域**: CORS
- **HTTP 客户端**: node-fetch

## 环境要求

- Node.js >= 16
- npm >= 8

## 环境变量

### 前端 (.env)
```
VITE_BACKEND_URL=http://localhost:3001
```

### 后端
```
PORT=3001
```

## API 文档

详见 [后端 README](./avatar-maker-backend/README.md)

## 许可证

MIT
